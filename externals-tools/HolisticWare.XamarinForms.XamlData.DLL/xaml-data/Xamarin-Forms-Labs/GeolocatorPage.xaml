<?xml version="1.0" encoding="UTF-8" ?>
<toolkit:BaseView
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:toolkit="clr-namespace:Xamarin.Forms.Labs.Mvvm;assembly=Xamarin.Forms.Labs"
  	x:Class="Xamarin.Forms.Labs.Sample.GeolocatorPage"
  	Title="Geolocator">
  <StackLayout>
  	<Button Text="Get Position" Command="{Binding GetPositionCommand}"></Button>
  	<Entry Text="{Binding PositionStatus}"></Entry>
    <Entry Text="{Binding PositionLatitude}"></Entry>
    <Entry Text="{Binding PositionLongitude}"></Entry>
    <Button Text="Send Position Email" Command="{Binding SendPositionEmail}"></Button>
    <Button Text="Send Position SMS" Command="{Binding SendPositionSms}"></Button>
    <Button Text="Open Position URI" Command="{Binding OpenPositionUri}"></Button>
    <Button Text="Driving Directions" Command="{Binding GetDrivingDirections}"></Button>
  </StackLayout>
</toolkit:BaseView>

